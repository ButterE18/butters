function runTimer(x){ //This function runs on the countdown

 var currTime = 50;
 var Timeout = 5000;
 
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
     x.innerHTML = currTime
     currTime = currTime - 5;
 }, Timeout);
 Timeout = Timeout + 1000;
 setTimeout(function(){
 alert('SIKE, Still no cookies for you, lmao XD!');
 }, Timeout);
}